import { DecimalSeparatorDirective } from './decimal-separator.directive';

describe('DecimalSeparatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalSeparatorDirective();
    expect(directive).toBeTruthy();
  });
});
